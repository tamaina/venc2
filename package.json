{
  "name": "venc2",
  "version": "0.0.0",
  "description": "A browser-based utility to downscale and transcode videos.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "tamaina <tamaina@hotmail.co.jp>",
  "license": "MIT",
  "packageManager": "pnpm@8.6.12",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "esbuild": "^0.25.8",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "build": "NODE_ENV=production node build.js && tsc --project tsconfig.build.json",
    "dev": "node build.js watch",
    "test": "pnpm -F test run dev",
    "test-build": "pnpm -F test run build",
    "preview": "pnpm -F test run build && pnpm -F test run preview"
  },
  "repository": "https://github.com/misskey-dev/browser-image-resizer.git",
  "dependencies": {
    "@misskey-dev/browser-image-resizer": "2024.1.0",
    "mp4box": "^1.4.6"
  }
}
